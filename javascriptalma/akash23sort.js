let number=[3,5,1,8,2]
 return number.sort(a,b);
console.log(number)

function double(numbers) {
    return numbers.map(function(n) {
    return n +100;
    });
    }f
    
    unction double(numbers) {
        return numbers.filter(n => n%===0).map(n => n /2);
        }